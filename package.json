{
  "name": "bingo-site",
  "version": "1.0.0",
  "description": "Website for $BINGO memecoin on XRP Ledger with integrated bingo roller",
  "main": "src/index.html",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/js",
    "format": "prettier --write src/**/*.{js,css,html}"
  },
  "dependencies": {
    "firebase": "^9.22.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0"
  }
}
